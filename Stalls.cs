namespace Theatre
{
    class Stalls : Zone // Партер
    {
        public Stalls(int _quantityRows, int _quantitySeats, double _price) :
            base("Stalls", _quantityRows, _quantitySeats, _price)
        { }
    }
}
